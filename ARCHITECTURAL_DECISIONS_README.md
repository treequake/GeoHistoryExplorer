# Architecture Decision Records

The active ADR directory is [ARCHITECTURAL_DECISIONS/](ARCHITECTURAL_DECISIONS/).

This top-level file is retained from the initial repository seed so existing references do not break. New decisions should be added as numbered files inside `ARCHITECTURAL_DECISIONS/`.
