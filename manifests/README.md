# Manifest Drafts

Manifest files describe GeoHistory Pack contents, build context, validation status, and known limitations.

The manifest is part of the scientific record. It should make a pack inspectable before any viewer or query layer is opened.
