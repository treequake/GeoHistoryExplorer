# Future Implementation Scaffold

This directory reserves implementation boundaries for later stages. Stage 0 should not build the full application.

Boundaries:

- `ingestion/`
- `normalization/`
- `storage/`
- `export/`
- `query/`
- `presentation/`

Each boundary should remain independently testable and inspectable as implementation begins.
